apiVersion: v1
kind: Service
metadata:
  name:  lists-web-svc
spec:
  selector:
    app:  lists-web
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: mainport
      name: lists-web-svc-http
